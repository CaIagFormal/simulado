spring.application.name=simulado

# Configura��es do servidor
server.port=8080

# Configura��es do banco de dados
spring.datasource.url=jdbc:postgresql://aulasprog.us.to:5432/DadabaseCarlos
spring.datasource.username=postgres
spring.datasource.password=senaipatobranco
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configura��es de JPA e Hibernate
spring.jpa.properties.hibernate.default_schema=contatos
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true